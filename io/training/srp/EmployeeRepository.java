package io.training.srp;

import java.awt.*;

public class EmployeeRepository {

    public void saveEmployee(Employee employee) {
//        DBConnection conn = Coonneciton.open("localhost:333", "user", "password");
//        conn.execute("INSERT INTO table_employee VALUE (" + employee.id + employee.age, employee.name, employee.salary + "");
//        conn.close();
    }


    public Employee findEmployee( Integer id ) {
//        DBConnection conn = Coonneciton.open("localhost:333", "user", "password");
//        Employee data = conn.execute("SELEC * FROM tabl_empoyee wHERE id = " + id );
//        conn.close();
//        return data;
        return new Employee();
    }




}
